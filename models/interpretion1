skip-gram: grad = y_pred - 1   #来源 负采样损失函数中的正样本损失对s(中间词向量与正确上下文词向量的乘积)的导数。
N-gram模型因其固定短窗口、数据稀疏性和缺乏全局上下文建模能力，无法有效处理长距离依赖。
加一平滑可能问题：
    加一平滑简单但会导致概率高估、分布不合理、困惑度升高和生成质量下降，尤其在小语料库或高阶N-gram中问题明显。
使用更高级的平滑方法或神经模型可改善效果。
